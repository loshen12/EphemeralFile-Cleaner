"""EphemeralFile Cleaner — 临时文件清理（回收站安全删除）。

跨平台 CLI（Windows / macOS / Linux）：以任务（目标目录 + 文件名正则组）为单位，
把文件名匹配任一正则的文件移入系统回收站；删除前自动备份并写入 manifest，
高危目录需二次确认，单次执行按小批量推进；支持长期任务清单与默认任务清单。
"""

__version__ = "1.0.0"
